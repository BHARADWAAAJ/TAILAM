{
  "name": "tailam",
  "version": "1.0.0",
  "private": true,
  "description": "TAILAM — Transformer Assessment for Insulating Liquid Analysis & Monitoring. Static, client-side only; this package.json exists solely for developer/CI tooling (engineering validation + UI smoke test) and installs nothing the deployed app depends on.",
  "scripts": {
    "validate": "node src/validation/validationRunner.js",
    "test:ui": "node src/validation/uiSmokeTest.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.0"
  }
}
